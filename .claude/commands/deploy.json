{
  "name": "deploy",
  "description": "自动部署到生产环境",
  "usage": "/deploy [commit-message]",
  "parameters": [
    {
      "name": "commit-message",
      "description": "自定义提交信息",
      "required": false
    }
  ],
  "script": "../scripts/auto-deploy.sh",
  "hooks": ["post-commit"],
  "enabled": true
}
