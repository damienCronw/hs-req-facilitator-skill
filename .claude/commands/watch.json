{
  "name": "watch",
  "description": "监控文件变化并自动部署",
  "usage": "/watch [interval]",
  "parameters": [
    {
      "name": "interval",
      "description": "检查间隔（秒，默认10秒）",
      "required": false
    }
  ],
  "script": "../watch-and-deploy.sh",
  "hooks": ["file-change"],
  "enabled": true
}
